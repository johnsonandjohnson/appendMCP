# Extract nominal p-value thresholds from OC object

Extract nominal p-value thresholds from OC object

## Usage

``` r
get_nominal_p(oc)
```

## Arguments

- oc:

  Operating characteristics object

## Value

Vector of nominal p-values
