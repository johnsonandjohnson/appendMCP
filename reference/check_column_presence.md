# Check that all objects have specified column

Check that all objects have specified column

## Usage

``` r
check_column_presence(objects, col_name, context)
```

## Arguments

- objects:

  Named list of data.frames

- col_name:

  Column name to check

- context:

  Context for error message
