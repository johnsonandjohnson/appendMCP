# Check allowed columns

Check allowed columns

## Usage

``` r
check_allowed_columns(df, name, allowed_cols)
```

## Arguments

- df:

  Data.frame to check

- name:

  Name of data.frame for error messages

- allowed_cols:

  Vector of allowed column names
